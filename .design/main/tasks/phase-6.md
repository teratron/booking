---
phase: 6
name: "Discovery, Reporting & Public API"
status: In Progress
subsystem: "app/Services/Seo, app/Services/Api, app/Http/Controllers/Api, app/Jobs, app/Filament/Admin"
requires: ["phase-3", "phase-5"]
provides: []
key_files:
  created: []
  modified: []
patterns_established: []
duration_minutes: ~
---

# Stage 6 Tasks — Discovery, Reporting & Public API

**Phase:** 6
**Status:** In Progress
**Strategic Goal:** Make the portal findable and its performance legible — URL grammar,
metadata, structured data, sitemaps and redirects; portal-wide reporting and exports;
and a versioned read-only REST contract behind issued tokens.

## Atomic Checklist

### Track A — Addressing Foundation

- [x] [T-6A01] Per-language slug resolution and the territory-hierarchy URL grammar
- [x] [T-6A02] Redirect table — model, administration, same-operation creation on slug change

### Track B — Metadata, Indexation, Structured Data & Sitemaps

- [x] [T-6B01] Per-language SEO metadata fields and the resolution ladder
- [x] [T-6B02] Indexation policy — the matrix, filter allowlist, pagination canonicals, robots
- [x] [T-6B03] Structured data and breadcrumbs, gated on module state
- [x] [T-6B04] Sitemap index — paginated per-language artefacts, regenerated by job
- [x] [T-6B05] SEO administration screen and health warnings

### Track C — Portal-Wide Reporting

- [x] [T-6C01] Derived figures across every aggregation dimension
- [x] [T-6C02] Traffic-source and page-popularity reporting in the back office

### Track D — Public API

- [x] [T-6D01] API module gate, versioned routing, and the disabled-capability 404
- [x] [T-6D02] API client and token model — issuance, scoping, revocation, journalling
- [x] [T-6D03] Read endpoints as a layer over the existing catalog retrieval contract
- [x] [T-6D04] Rate limiting, consumption measurement, and generated documentation

### Track T — Validation

- [x] [T-6T01] Indexation invariant — nothing non-public is ever indexable
- [ ] [T-6T02] API parity invariant — never a visibility or ordering the site would not give
- [x] [T-6T03] Redirect permanence and slug stability

## Task Detail

### Track A — Addressing Foundation

**[T-6A01] Per-language slug resolution and the territory-hierarchy URL grammar**
Replace the placeholder public addressing with the specified grammar: territory paths
mirror the hierarchy (`/{lang}/{country}/{region}/{district}/{settlement}` and a typed
catalog beneath it), while object profiles sit on a deliberately **flat**
`/{lang}/o/{object-slug}` so that recategorizing or reassigning an object never breaks
a ranked URL. Resolve through a single indexed lookup on language, entity type, and
slug. The denormalized `territory_translations.full_slug_path` column already exists
and is written by the territory administrator on reparenting — this task makes it the
read path, backfills the rows no administrator has touched, and adds the uniqueness
constraint the lookup assumes.
**One host, language as the leading path segment — settled, not open.** There are no
language subdomains and no per-country domains. Alternates are therefore path swaps on
the same host, canonicals always name that host, and no part of this task should be
built to keep a subdomain migration cheap.
*Verify:* `docker compose exec app php artisan test --filter=SeoUrlGrammarTest` — asserts
a five-level territory path resolves to the right record, that an object URL is
unchanged by reparenting its territory, and that two territories in different countries
may share a leaf slug. Plus `composer quality` green with all 21 existing call sites of
the former route names updated.

**[T-6A02] Redirect table — model, administration, same-operation creation on slug change**
Give the existing `redirects` table its model, its administrator-editable resource, and
the resolution middleware that consults it on a slug miss before rendering 404. Wire
redirect creation into the same operation as the slug change, for slug edits, territory
reparenting, and archived content — a redirect added in a later step is added after the
traffic is already lost.
*Verify:* `docker compose exec app php artisan test --filter=RedirectResolutionTest` —
asserts a changed slug serves 301 from the old path, that the redirect row is created
within the same transaction as the slug write, and that a slug is never reused while a
redirect claims it.

### Track B — Metadata, Indexation, Structured Data & Sitemaps

**[T-6B01] Per-language SEO metadata fields and the resolution ladder**
Today only `seo_title` and `seo_description` exist on the translation tables. Add the
rest of the per-language set the specification requires — canonical URL, indexation
directive, Open Graph title, description and image, and the search-engine text block —
across every entity that needs it, then build the three-step resolution ladder:
explicit value, else an administrator-defined template rendered from entity data, else
derived from the entity name and its territory. Empty metadata is never shipped.
*Verify:* `docker compose exec app php artisan test --filter=MetadataResolutionTest` —
one case per ladder rung, asserting the template rung fires only when the explicit value
is absent and that no rendered page emits an empty title or description.

**[T-6B02] Indexation policy — the matrix, filter allowlist, pagination canonicals, robots**
Make indexability a data-driven per-page attribute the rendering layer reads, never a
hard-coded per-route decision. Filtered catalog views are **not indexable by default**;
a filter combination becomes indexable only through an explicit administrator entry in
an allowlist. Paginated views beyond page 1 are indexable and canonical to themselves,
not to page 1. Non-public objects, the owner cabinet, and the back office are excluded
and disallowed in an administrator-manageable robots directive.
*Verify:* `docker compose exec app php artisan test --filter=IndexationPolicyTest` — a
case per row of the specification's indexation matrix, including that a single-filter
catalog URL emits `noindex` until allowlisted and page 2 canonicalizes to itself.

**[T-6B03] Structured data and breadcrumbs, gated on module state**
Emit type-appropriate structured data — lodging, food establishment, place, article,
offer — plus a breadcrumb list on every page below home. Gate offer-availability
emission on booking-module state for that object: emitting availability the portal
cannot honour is a durable trust penalty, not a cosmetic error.
*Verify:* `docker compose exec app php artisan test --filter=StructuredDataTest` —
asserts each page type emits its own entity, that breadcrumbs are present below home,
and specifically that an object page emits **no** offer availability while the booking
module is disabled.

**[T-6B04] Sitemap index — paginated per-language artefacts, regenerated by job**
A sitemap index referencing per-language, per-entity-type sitemaps, each paginated
within the format's limits, written as static artefacts by a scheduled job rather than
computed per request. Only indexable pages appear. Requires adding
`spatie/laravel-sitemap`, which the project's package list names but which is not yet
installed.
*Verify:* `docker compose exec app php artisan test --filter=SitemapGenerationTest`
against the realistic-volume seeder — asserts the index references one child per
language and entity type, that pagination triggers past the format limit, and that a
`noindex` page never appears in any child.

**[T-6B05] SEO administration screen and health warnings**
The back-office surface where an SEO specialist manages titles, descriptions, addresses,
canonicals, robots directives, Open Graph data, territory page copy, filter indexation,
title templates, the sitemap, redirects, error pages, and structured data — plus the
health warnings that make a catalog this size maintainable: missing title, missing
description, duplicate address, missing translation, over-length title, and page
excluded from indexing. At portal scale nobody finds a duplicate slug by browsing.
*Verify:* `docker compose exec app php artisan test --filter=SeoAdministrationTest` —
asserts each of the six warning types fires against a deliberately broken fixture, and
that the screen is permission-gated for a user without the SEO grant.

### Track C — Portal-Wide Reporting

**[T-6C01] Derived figures across every aggregation dimension**
`AnalyticsReport` already serves period-scoped counts filterable by object, banner,
territory, country, type and language. What is missing is the derived set the
specification names: most viewed objects, most popular categories, banner
click-through rate, new owner count, new object count, bump count, published promotion
count, and pending moderation count. Compute them against the aggregate tier only,
never against raw events, and keep them exportable through the existing filter set.
*Verify:* `docker compose exec app php artisan test --filter=PortalReportingTest` — one
assertion per derived figure against a fixed fixture with known counts, plus a query-count
assertion proving no figure reads `stat_events`.

**[T-6C02] Traffic-source and page-popularity reporting in the back office**
`TrafficSourceRecorder` already records a coarse channel, referring host, and campaign
tag on a visit's first event. Nothing surfaces it. Aggregate it by channel in the back
office alongside page popularity — never as a per-visitor list, which is what the
coarse shape exists to prevent.
*Verify:* `docker compose exec app php artisan test --filter=TrafficSourceReportTest` —
asserts the six channels aggregate correctly, that the internal channel is reported
distinctly from referral, and that no endpoint or export can return a per-visitor row.

### Track D — Public API

**[T-6D01] API module gate, versioned routing, and the disabled-capability 404**
Register the API as a gateable module, disabled at portal scope by default — the module
registry currently carries only reviews, guest accounts, booking, and payment. Add the
versioned route file and its bootstrap registration. A request against a disabled module
returns **404, not 403**: a 403 confirms the capability exists.
*Verify:* `docker compose exec app php artisan test --filter=ApiModuleGateTest` —
asserts 404 with the module disabled and a non-404 with it enabled, across both an
existing and a non-existing endpoint so the two are indistinguishable when disabled.

**[T-6D02] API client and token model — issuance, scoping, revocation, journalling**
The `api_clients` table and Sanctum's `personal_access_tokens` both already exist from
the schema pass; neither has a model. Build the client and token models, the
administrator resource that registers a client and issues a scoped, expiring token, and
the revocation path that takes effect immediately rather than at next expiry. A token's
scope — resources, countries, categories — resolves through the same scoping model as
administrative permissions, not a second authorization system. Store hashes only;
display the clear value once. Journal every issuance, revocation, and scope change.
*Verify:* `docker compose exec app php artisan test --filter=ApiTokenLifecycleTest` —
asserts a revoked token is rejected on its very next request, that the clear value is
absent from storage, and that all three lifecycle events appear in the action journal.

**[T-6D03] Read endpoints as a layer over the existing catalog retrieval contract**
The read-only collection and detail endpoints, built as a layer over
`CatalogQueryService` rather than a parallel query path — two implementations of
"list objects in a territory, tier-ordered" will diverge, and the divergence is a
revenue bug. Collections return the same ordering the catalog renders. Apply the
published-only visibility filter in the shared query layer, not per endpoint. Responses
are language-negotiated and fall back exactly as pages do.
*Verify:* `docker compose exec app php artisan test --filter=ApiReadContractTest` —
asserts every collection endpoint's ordering matches the catalog's for the same filter
set, and that a pending, rejected, archived, or hidden record is absent from every
endpoint.

**[T-6D04] Rate limiting, consumption measurement, and generated documentation**
Per-token rate limiting with per-token configurable limits, applied **before** the query
runs — a limiter that runs post-query offers no protection to the database it protects.
Record consumption per token and per endpoint through the existing event pipeline.
Publish documentation generated from the same schema definitions that validate requests,
at a stable versioned address, so contract and documentation cannot drift.
*Verify:* `docker compose exec app php artisan test --filter=ApiRateLimitTest` — asserts
429 with a retry hint at the configured limit, a query-count assertion proving the
limiter short-circuits before any catalog query, and that the generated document lists
every registered route.

### Track T — Validation

**[T-6T01] Indexation invariant — nothing non-public is ever indexable**
A single test that sweeps every registered public route and asserts the negative case
holistically rather than per-task: no pending, rejected, archived, or hidden object, no
cabinet or back-office path, and no multi-filter catalog URL is ever indexable or
present in any sitemap.
*Verify:* `docker compose exec app php artisan test --filter=IndexationInvariantTest` —
route-registry driven, so a route added later without an indexation decision fails the
test rather than shipping silently.

**[T-6T02] API parity invariant — never a visibility or ordering the site would not give**
Assert the API and the website agree, by comparing them directly: for a matched filter
set, the API's result identity and order equal the catalog page's, and any record absent
from the page is absent from the API. This is the test that would catch a "neutral
ordering" regression, which is a revenue defect rather than a display bug.
*Verify:* `docker compose exec app php artisan test --filter=ApiParityInvariantTest`
against seeded volume, comparing API payload order against the rendered catalog's own
retrieval for at least three filter permutations including an unfiltered first page.

**[T-6T03] Redirect permanence and slug stability**
Assert the durable promises: a changed slug always leaves a permanent redirect, a slug
is never silently reused, a redirect chain resolves rather than looping, and territory
reparenting leaves every affected descendant path redirected.
*Verify:* `docker compose exec app php artisan test --filter=SlugStabilityInvariantTest`
— includes a reparent of a mid-tree territory with descendants, asserting a redirect
exists for every descendant path, and a deliberate two-hop chain asserting no loop.

## Track Ordering

**Phase 6 is three-wide: `(A → B) ∥ C ∥ D → T`.** Unlike Phase 5, which funnelled
through a single retrieval gate, this phase has three largely disjoint concerns — and
saying so precisely matters, because two of them can start immediately while the third
cannot.

`T-6A01` is the phase's hard gate and its highest-cascade task, but it differs in kind
from `T-5A03` or `T-2A02`: those were greenfield contracts adopted by later work, while
this one is a **retrofit** of addressing that 21 existing files already depend on.
Every task in Track B consumes the URL grammar — canonical URLs, sitemap entries, and
breadcrumbs are all statements about addresses — so Track B cannot begin until it lands.
`T-6A02` also depends on it, since a redirect's target is expressed in the new grammar.

Within Track B the ordering is `B01 → B02 → (B03 ∥ B04) → B05`. `T-6B01` adds the
indexation directive that `T-6B02` governs; `T-6B04` may only include pages `T-6B02` has
declared indexable; `T-6B05` administers the fields all four establish, so it lands last.

**Track C is fully independent** of A, B, and D. It reads `stat_dailies`, which Phase 3
built and Phase 4 already reports against — no task here touches a route, a URL, or a
token. It can run first, last, or concurrently.

**Track D is independent of A and B.** The API is a layer over `CatalogQueryService`
(Phase 5) and does not consume the SEO URL grammar — its resources are addressed by
identifier, not by slug path. Internally it is a strict chain: `D01 → D02 → D03 → D04`.
The module gate must exist before tokens are checked behind it, tokens before endpoints
are scoped, and endpoints before a limiter or a generated document can describe them.

One cross-track contract is scheduled rather than discovered mid-run: **`T-6B02` and
`T-6D03` must share one published-only visibility filter, not build two.** Both specs
ask for the same thing in their own language — the indexation policy excludes non-public
records, and the API applies its visibility filter in the shared query layer. Two
implementations would drift, and the drift would expose unmoderated content on whichever
side was forgotten. Whichever task lands first owns the filter; the second consumes it.

Track T runs last. All three of its invariants are cross-track by construction —
`T-6T01` sweeps the route registry Track B populates, `T-6T02` compares Track D against
Phase 5's own rendering, and `T-6T03` exercises Track A's redirect promises against
territory operations built back in Phase 2.

## Planning Audit

**Optimism bias.** `T-6A01` is the one to distrust. Read as "add some routes" it looks
small; it is a rewrite of the addressing every public page built in Phase 5 depends on,
and each of that phase's feature tests asserts against generated URLs. Two things
genuinely reduce it: `territory_translations.full_slug_path` already exists, and the
public route group was deliberately built as a swappable scaffold with the eventual
grammar noted at the call site. What remains real is the reverse resolution, the
backfill for territories no administrator has ever edited (only one code path writes
that column today), and the uniqueness constraint the lookup will assume.

`T-6B04` is the second underestimation risk. At the seeded volume this project
benchmarks against, sitemap generation across several entity types in every active
language is a paginated batch job, not a call to a helper — and the package it needs is
named in the project's own package list but is not installed.

`T-6D03` reads like eleven endpoints of routine serialization. The actual risk is that
`CatalogQueryService` was built for a Blade and Livewire caller; exposing it through a
JSON contract without either duplicating the query or leaking internal shapes is the
task's real content.

**Hidden dependencies.** Claiming four-way parallelism here would be false in the same
way it was for Phase 1. The honest shape is three-wide, and Track D's internal chain is
strictly sequential, so the effective parallel degree is three at the start and drops to
two once Track C finishes — it is the shortest track by a wide margin. The shared
visibility filter between `T-6B02` and `T-6D03` is the one genuine cross-track resource
bottleneck, which is why it is written into §Track Ordering rather than left to be
discovered when the two implementations disagree.

**Cascade risk.** `T-6A01` blocks all five Track B tasks and one Track A task — six of
sixteen — and a broken landing fails every Phase 5 feature test simultaneously, which
would make the failure look like a regression in work that is already complete. Running
it against the existing public test suite rather than only its own new test is the
mitigation. `T-6D01` carries the second-largest blast radius relative to its size: the
404-versus-403 decision is security-adjacent, cheap to get wrong, and invisible until
someone probes for it, which is precisely why `T-6T02` exists as a separate invariant.

**Plan stability.** All three specifications remain `RFC`, as every phase before this
one was planned and executed under. Two open questions touched this phase at planning
time; **one is now closed.** The deferred question of country-specific domains — the
one genuinely consequential for a task rather than merely for a document, since
`T-6A01` is what would have made the answer permanent — was settled by the project
owner on 2026-08-15: **a single host, with language as the leading path segment, and no
subdomains of any kind.** That removes the phase's only irreversible fork. It also
means `T-6A01` should not carry defensive structure to keep a subdomain migration
mechanical; the specification's §2 note and `l1-localization.md` §7 both anticipate a
migration that is no longer planned, and building for it would be paying for optionality
the owner has declined.

The remaining question — the absence of any stated rate-limit figure or data-licensing
position — is a business decision `T-6D04` should pick a defensible default for rather
than wait on, since the design is deliberately conservative enough that opening it later
is a policy change rather than a redesign.

## Scope

| Area | Spec |
| --- | --- |
| URL grammar across languages and the territory hierarchy | l1-seo.md §5.1 |
| Metadata resolution with administrator-defined templates | l1-seo.md §5.2, §3.3 |
| Indexation policy for filtered and paginated catalog views | l1-seo.md §5.3, §3.2 |
| Structured data per page type, and the no-overstatement rule | l1-seo.md §5.4, §3.4 |
| Sitemap index, pagination, regeneration job, redirect table | l1-seo.md §5.5 |
| Back-office SEO administration and warnings | l1-seo.md §5.6; l1-back-office.md §5.1 |
| Portal-wide reporting across every aggregation dimension | l1-analytics.md §5.3, §5.4 |
| Traffic sources and page popularity | l1-analytics.md §5.6 |
| Versioned read contract mirroring the catalog's ordering | l1-public-api.md §5.1 |
| Token model, scoping, revocation, rate limits | l1-public-api.md §5.2, §5.3 |
| Generated API documentation | l1-public-api.md §5.4 |
| API client and token administration | l1-public-api.md §5.5 |

## Standing Constraints

- Filtered catalog views are **not indexable by default**. A filter combination becomes
  indexable only by explicit administrator decision — this is the portal's principal
  defence against near-duplicate proliferation across three dimensions that multiply.
- Structured data must not overstate. Where the booking module is inactive, an object
  page must not emit offer availability it cannot honour.
- Collection endpoints return the **same ordering the catalog renders**. A neutral
  ordering would let a consumer build a competing listing that bypasses the portal's
  revenue model entirely.
- The API exposes published, public data only — at every tier, for every token.
- A disabled API module returns 404, not 403: a 403 confirms the capability exists.
- Slug changes create a permanent redirect; slugs are never silently reused.
- **Object URLs stay flat** (`/{lang}/o/{slug}`) while territory URLs mirror the
  hierarchy. An administrator may move an object between cities and resorts, and under a
  hierarchical object URL every such move would break a ranked page.
- **One host; language is the leading path segment; no subdomains.** Not a default
  awaiting review — a settled decision. Alternates are path swaps on the same host, and
  neither language subdomains nor per-country domains are to be built toward.
- **One published-only visibility filter, shared** between the indexation policy and the
  API's query layer. One forgotten filter exposes unmoderated content.
- Rate-limit before doing work. A limiter that runs after the query protects nothing.
- The API read contract layers over `CatalogQueryService`; it never opens a second
  retrieval path.
- Sitemaps are generated artefacts written by a job, never computed per request.

## Detailed Tracking

### [T-6A01] Per-language slug resolution and the territory-hierarchy URL grammar

- **Spec:** l1-seo.md §5.1, §6.1
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest tests/Feature/Public/SeoUrlGrammarTest.php` — 8 cases: a five-level territory path resolves correctly; the typed-catalog trailing segment disambiguates from a plain territory path; an object's own URL survives both a direct territory reassignment and a territory reparent; two territories in different countries share an identical leaf slug and full path without collision; the bare country segment redirects to its own landing territory; an unknown/inactive country 404s; a territory path resolved against the wrong country segment 404s. Full non-slow suite: 668 passed, 0 failed, 3 skipped. `composer analyse` (PHPStan level 8), `pint --test`, and both architecture tests (including `ContainmentTest`) clean.
- **Handoff:** All of Track B (metadata, indexation, sitemaps) builds on this grammar. `T-6A02` (redirects) depends on it directly — a redirect's target is expressed in this same grammar.
- **Notes:** The retrofit turned out larger than the original task description anticipated, for three compounding reasons found only by reading the schema rather than assuming it: (1) `Country` and `ObjectType` carried no slug at all — the country segment uses the country's own stable ISO code instead (no per-language country name exists in the spec), and `object_type_translations` gained a new per-language `slug` column for the typed-catalog segment; (2) `territory_translations`' original `unique(locale, slug)` constraint was already flagged at creation time as an approximation and had to be replaced with the real boundary; (3) that real boundary turned out to be `(locale, country_id, full_slug_path)`, not `(locale, full_slug_path)` alone — a bare path-only constraint rejects two identically-named root territories in different countries, which is exactly the case the spec promises to allow. `country_id` is denormalized onto `territory_translations` (Postgres cannot scope a unique index through a foreign key's own parent table) and kept in sync by `TerritoryAdministrator::recomputeSlugPaths()`, the one place that already writes `full_slug_path`. `PublicSlugResolver`/`PublicUrlGenerator` (new, `app/Services/Seo/`) are the sole inbound/outbound halves of the grammar; every prior direct `route('public.territories.show', …)`/`route('public.objects.show', …)` call site (21 files: controllers, Blade views, `ObjectCardPresenter`, `PublicShellDataProvider`) now goes through them. The typed-catalog page (`{settlement}/{type}`) is a new, minimal server-rendered paginated listing — deliberately not the full interactive filter UI `/catalog` already provides, since the clean path is what makes it eligible for indexing at all (Track B's own concern, not re-litigated here).
- **Changes:** Two new migrations: `add_seo_slug_support_to_object_types_and_territories` (slug on `object_type_translations`; replaces `territory_translations`' `unique(locale, slug)` with `unique(locale, full_slug_path)`) and `scope_territory_slug_path_uniqueness_by_country` (adds `territory_translations.country_id`, backfills it, replaces the constraint again with `unique(locale, country_id, full_slug_path)`). New `App\Services\Seo\PublicUrlGenerator` and `App\Services\Seo\PublicSlugResolver`. New `CountryLandingController` (bare `/{lang}/{country}` redirects to that country's own top-level landing territory, reusing the same resolution `CountryPreferenceController` already had). `TerritoryPageController::show()` now resolves `(country, path)` via the resolver and branches to either the existing territory-landing composition or the new typed-catalog view. `ObjectPageController::show()` resolves a flat slug instead of binding `Object_` by id. `ObjectType` gained `slug` as a translated attribute; its Filament form/edit pages gained the field. `PublicShellDataProvider`'s `PublicDestinationOption` DTO gained a pre-resolved `url`, computed at cache-build time (the DTO never carried enough — country, full path — for a view to resolve it itself). `TerritoryAdministrator::recomputeSlugPaths()` now also syncs `country_id`. `DemoVolumeSeeder` computes `full_slug_path`/`country_id` inline during tree construction rather than a second pass, with the per-node path map reset at each country boundary (bounds its memory to one country's tree, not the whole run).
- **Evidence:** New `tests/Feature/Public/SeoUrlGrammarTest.php` (8 cases, listed above). Two real bugs caught by PHPStan before they shipped: an inverted boolean (`! Str::lower(...) === Str::lower(...)`, operator precedence) in the resolver's country check would have accepted every country unconditionally, caught by manual re-read before the first test run; a nullable-relation type mismatch passing `$promotion->object`/`$promotion->territory` into the generator's non-nullable parameters, fixed with an explicit `instanceof` guard backed by the domain invariant that a promotion always carries both. One real functional gap found only by running the full suite, not by design review: the Filament `ObjectType` create/edit form had no slug field at all — creating or editing a type through the admin panel would have failed outright the moment `slug` became `NOT NULL`; fixed in the same change (`ObjectTypeForm`, `EditObjectType::mutateFormDataBeforeFill()`). A second full-suite pass surfaced the country-scoped uniqueness gap directly (a test seeding two identically-named root territories in different countries hit a real unique-constraint violation), which is what drove the second migration rather than leaving the boundary too narrow. Full non-slow suite (668/0/3), full architecture suite (8/8 including `ContainmentTest`), `composer unused` (0 unused), `composer audit` (clean) all independently re-verified after the fix. The `slow`-group suite reproduces `DemoVolumeSeederTest`'s own pre-existing, documented memory-ceiling flake (STATE.md) under an artificial 128 MB PHP limit designed to stress its chunking — confirmed unrelated to this task's own changes (the container's real memory limit is 3.7 GB, in active use at ~130 MB baseline) and left as-is rather than papered over; `PlacementOrderingVolumeTest` and `PublicPerformanceBudgetTest` (65 objects and 30 territory-page queries respectively, both within their stated budgets) pass cleanly in the same group.

### [T-6A02] Redirect table — model, administration, same-operation creation on slug change

- **Spec:** l1-seo.md §5.5, §6.5
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest tests/Feature/Public/RedirectResolutionTest.php` — 6 cases: a territory slug edit through the real admin form creates a redirect in the same operation, and the old path serves a 301 while the new path serves 200; the same for an object slug edit; a territory reparent creates a redirect covering the reparented node's own path; a slug reuse that collides with an active redirect's `from_path` is refused; a redirect chain of two hops collapses to one (the first hop's `to_path` is repointed, not left stale); the admin resource is reachable and lists a created row. Full non-slow suite: 674 passed, 0 failed, 3 skipped. `composer analyse`, `pint --test`, and the architecture suite clean.
- **Handoff:** None within this phase — the redirect table's own resolution and administration are now complete infrastructure. `T-6T03` (redirect permanence and slug stability invariant) exercises this task's own promises directly rather than building new ones.
- **Notes:** Two real bugs surfaced only by running the actual test against the actual HTTP pipeline, not by code review — recorded because neither is obvious from reading Laravel's own documentation. First: the original middleware design wrapped `$next($request)` in a `try { } catch (NotFoundHttpException $e) { … }`, which never fired — `Illuminate\Routing\Pipeline` (the pipeline Laravel builds specifically for route-group middleware) overrides exception handling to catch and render an exception into a Response *inside* `$next()` itself, before it would ever propagate to an outer `catch`. The fix inspects `$response->getStatusCode() === 404` instead of catching anything; this is the general shape any future middleware in this codebase wanting to intercept a downstream 404 needs, not specific to redirects. Second: `TerritoryAdministrator::recomputeSlugPaths()` was not wrapped in its own transaction — called directly (not only via `reparent()`, which already had one), a refused reuse partway through a multi-node subtree would have left earlier nodes in that same call already rewritten. Fixed by wrapping the whole method body in `DB::transaction()`; nesting inside `reparent()`'s own transaction is safe (Laravel promotes it to a savepoint). The "same operation" requirement turned out to have two distinct object-side write paths, not one: the admin panel's direct edit (`EditObject::handleRecordUpdate()`) and a moderated (owner-submitted, staff-approved) edit, which only actually writes inside `ModerationDecisionService::applyData()` — a shared, generic method already used by every moderated content type. Wired both, plus territory edits and reparenting via `TerritoryAdministrator::recomputeSlugPaths()` (one call site covering both cases, since reparenting already routes through it).
- **Changes:** New `App\Models\Redirect`, `App\Policies\RedirectPolicy`, `App\Services\Seo\RedirectRegistrar` (the sole write path — `registerPathChange()` creates or repoints a redirect and collapses an existing chain into it in the same call; `isClaimed()` is the no-reuse guard), `App\Http\Middleware\ResolveRedirect` (wraps the public `{lang}` route group, consulted on any 404), `App\Exceptions\SlugReuseRefusedException`, and a full `RedirectResource` (Filament Admin, `geography` navigation group, `settings.*` permissions — portal-wide configuration, not scoped). `TerritoryAdministrator::recomputeSlugPaths()` now registers a redirect per translation whose `(country, path)` actually changed, and refuses (before writing) a path an active redirect already claims. `EditTerritory`/`EditObject` (Admin) both register a redirect on a slug save and show a danger notification (skipping just that locale's write, not the whole form) on a refused reuse. `ModerationDecisionService::applyData()` gained the same two behaviours generically for `Object_` targets specifically, via `stripClaimedSlugs()` (drops a claimed proposed slug before `fill()`, letting the rest of a moderated change still apply) and `registerObjectSlugRedirects()`.
- **Evidence:** New `tests/Feature/Public/RedirectResolutionTest.php` (6 cases, listed above). `tests/Feature/Admin/PanelAuthorizationMatrixTest.php` required updating — it enumerates every axis-less (portal-wide) Filament resource by reflection and asserts the exact set, specifically so a newly added global resource cannot silently go untested; added `RedirectResource` to both the expected set and its own probe-row fixture. Full non-slow suite: 674 passed, 0 failed, 3 skipped (up from 668 — 6 new, 0 regressed). Full architecture suite (8/8, `ContainmentTest` clean) and `PanelAuthorizationMatrixTest` (5/5, including the two country-scope-refusal assertions extended to cover the new resource) independently re-verified after the fix.

### [T-6B01] Per-language SEO metadata fields and the resolution ladder

- **Spec:** l1-seo.md §3.3, §5.2
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest tests/Feature/Public/MetadataResolutionTest.php` — 10 cases: explicit value wins over a seeded template; the template rung renders and substitutes `{name}`/`{territory}` only when no explicit value exists; the derivation rung produces a non-empty title/description from the entity's own name when neither exists; a real territory page never ships an empty `<title>` or meta description for a bare entity; Open Graph title/description mirror the resolved title/description when unset, and an explicit Open Graph override wins over the mirror; `seo_indexable` emits `noindex` only when explicitly false, never by default; the canonical link falls back to the page's own resolved URL and honours an explicit override; the typed-catalog composite (territory × object type) resolves through the object type's own template composed with the territory name, and derives correctly when no template exists. Full non-slow suite: 449 passed (Public/Admin/Cabinet directories), 0 failed. `composer analyse` (PHPStan level 8), `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass.
- **Handoff:** `T-6B02` (indexation policy) consumes `seo_indexable`'s raw per-entity override as one input into its own broader matrix (filter combinations, pagination, non-public exclusion) — this task deliberately does not build that matrix itself. `T-6B04` (sitemaps) and `T-6B05` (SEO administration screen) both read the `seo_metadata_templates` table this task creates; `T-6B05` is the first to give it a CRUD UI — until then, templates are seeded defaults and admin-editable only by direct database access.
- **Notes:** Two real, non-obvious discoveries, both found only by running actual code rather than by design review. First: `TerritoryForm`'s per-language SEO section carried a `seo_title` field but no `seo_description` field at all — a pre-existing gap from the schema pass, silently meaning a territory's meta description could only ever be database-seeded or blank, never administrator-edited; fixed as part of the same shared-field extraction rather than left in place. Second, and more consequential: the naive implementation (one `seo_metadata_templates` query per page render when an entity lacks an explicit title/description) pushed the territory page's own cache-miss query count from the project's tuned baseline of exactly 30 to 31, failing `PublicPerformanceBudgetTest`'s hard ≤30-query ceiling — `Cache::rememberForever()` alone does not fix a *first-ever-request* measurement, since the cache is still genuinely cold for that one call. The real fix follows the same pattern the performance test's own `perfBudgetWarmShellCaches()` helper already established for `PublicShellDataProvider`'s navigation/language/country caches: an administrator-edited, traffic-independent lookup is warmed once before request-scoped measurement, not paid for on a specific page's own miss budget. `MetadataResolver::warmTemplateCache()` is the new warming entry point (paired with `forgetTemplateCache()` for the write side `T-6B05` will need), and the perf test now warms the territory-type template cache alongside the pre-existing shell caches. Re-verified: territory page cache-miss query count is exactly 30 (at budget, zero regression) after the fix. Also caught by Pint's own auto-import behaviour (a recurring, previously-documented gotcha in this project): an `{@see MetadataResolver}` docblock reference in the new `SeoMetadataTemplate` model would have imported a service class into a model file, failing the "models are thin" architecture test — restated in prose instead, in all four places it recurred across this task's new files, before the architecture suite ever saw it. A second containment leak — `[l1-seo.md]` spec-filename references copied into three new files' own docblocks — was caught by a manual re-check before `ContainmentTest` ran, not by the test itself catching it after the fact.
- **Changes:** Two new migrations: `add_seo_metadata_fields_to_translation_tables` (five new nullable columns — `seo_canonical_url`, `seo_indexable`, `seo_og_title`, `seo_og_description`, `seo_og_image` — added individually per literal table name across all six SEO-bearing translation tables, since the project's schema-aware static analysis only recognizes a literal `Schema::table('name', …)` call, not one resolved through a loop variable) and `create_seo_metadata_templates_table` (the middle ladder rung's own small table: `entity_type`, `locale`, `field`, `template`, unique per triple). New `App\Services\Seo\MetadataResolver` (the three-rung ladder: explicit column → administrator template → name/territory derivation, applied to title and description, with canonical/indexable/Open Graph resolved alongside), `App\Models\SeoMetadataTemplate`, and `App\Support\Seo\{SeoEntityType,SeoMetadataField,SeoEntityContext,ResolvedMetadata}`. New `App\Filament\Support\SeoMetadataFields` — the shared seven-field Filament schema fragment (title, description, canonical override, a three-state indexable select rather than a plain toggle, since a toggle cannot distinguish "no override" from an explicit "off", and the three Open Graph fields) reused across all six entity forms plus the owner cabinet's own object form, replacing six near-duplicate inline field blocks. `components/layouts/public.blade.php` now accepts a `:metadata` prop and emits meta description, canonical link, conditional `noindex`, and Open Graph tags — previously only a bare `<title>` existed, with no description, canonical, or Open Graph markup anywhere on the public site. Five public controllers (`TerritoryPageController` — both the plain landing and the typed-catalog composite, `ObjectPageController`, `PromotionController`, `NewsController`, `BlogController`) now resolve and pass real metadata instead of the raw entity name/title they passed before. New `database/seeders/SeoMetadataTemplateSeeder`, registered in `DatabaseSeeder`, seeding starting EN/RU templates for every active language found in the registry (never a hardcoded language list). New `seo.default_og_image` portal setting (`SettingsRegistry`), the Open Graph image fallback of last resort.
- **Evidence:** New `tests/Feature/Public/MetadataResolutionTest.php` (10 cases, listed above). `tests/Feature/Public/PublicPerformanceBudgetTest.php` required a fix, not just a passing re-run — its `perfBudgetWarmShellCaches()` helper now also warms the SEO template cache, and the territory page's cache-miss query count is independently re-verified at exactly 30 (previously 31, a real regression this task caused and then fixed within the same task rather than shipping over budget). Full `tests/Feature/Public`, `tests/Feature/Admin`, `tests/Feature/Cabinet` run: 449 passed, 0 failed (10 new from this task, 0 regressed). Full architecture suite (8/8, including `ContainmentTest`) and `composer analyse`/`pint --test` independently re-verified clean after both containment-leak and Pint-auto-import fixes. `composer unused` (0 unused) and `composer audit` (clean) re-verified.

### [T-6B02] Indexation policy — the matrix, filter allowlist, pagination canonicals, robots

- **Spec:** l1-seo.md §3.2, §5.3
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest tests/Feature/Public/IndexationPolicyTest.php` — 7 cases: the plain, unfiltered `/catalog` view is indexable by default; a single-filter catalog URL emits `noindex` until an administrator promotes that exact filter value, then indexes; two or more active filters never index, even when each is individually promoted; a free-text search never indexes regardless of promotions; catalog pagination beyond page 1 stays indexable and canonicalizes to its own page, not page 1; a non-published object 404s with `noindex` (an existing structural guarantee, reconfirmed rather than rebuilt); `robots.txt` disallows `/admin` and `/cabinet`. Full non-slow suite: 691 passed (up from 684), 0 failed, 3 skipped. `composer analyse`, `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass. Catalog page cache-miss query count: 26 (within the 30 budget, `T-6B01`'s own lesson about zero-headroom pages did not repeat here since the catalog page had 8 queries of headroom).
- **Handoff:** `T-6B03` (structured data) and `T-6B04` (sitemaps) both read `ResolvedMetadata::$indexable` — sitemaps must include only what this task marks indexable. `T-6T01` (the portal-wide indexation invariant) sweeps every registered route against this task's own matrix rather than re-deciding it; a route added later without an explicit indexation decision is exactly what that later test exists to catch. `T-6B05`'s administration screen is the first to give `catalog_filter_promotions` and `seo.robots_extra` a purpose-built UI — until then, filter promotions are direct-database-only and the robots directive is editable through the existing generic Portal Settings screen.
- **Notes:** The catalog's filter space is open-ended (object type, territory, amenities, price range, rating, type-declared attributes, free text) but the spec's own promotion rule collapses that: only ever *one* active filter is promotable, several are never promotable regardless of allowlist state. That collapse is what keeps the allowlist small and generic — one `signature` string (`{key}={value}`) per promotion, rather than typed columns per filter kind, which would have meant a schema change for every future filter dimension. A free-text search (`q`) is treated as an automatic non-indexable state, never promotable — not named as its own row in the spec's matrix, but a direct application of the same near-duplicate-proliferation principle §7 states as this policy's whole reason to exist (infinite query variations, no meaningful "combination" to promote). Continuous-range filters (price, rating) are allowlistable by construction (their signature is just their own value), but this task does not build a rounding/bucketing scheme for them — a real product decision for `T-6B05`'s screen to make, not this task's to preempt. The `/catalog` page turned out to be a Livewire full-page component (`CatalogSearch`), not a plain controller — passing it computed metadata needed Livewire's own `View::layoutData()` macro (`->layoutData(['metadata' => $metadata])`) rather than the plain array-return-to-view pattern the other five controllers use; discovered by reading Livewire's `SupportPageComponents` feature source, since nothing in this codebase's own public pages does this yet. Pagination's own "canonical to itself" requirement needed no new mechanism at all: it already falls out of always building canonical from the page's actual current URL (`url()->full()`, page number included) rather than a normalized or stripped one — the risk was accidentally stripping the page parameter, not needing to add one.
- **Changes:** New migration `create_catalog_filter_promotions_table` (`signature` unique string, `is_active`). New `App\Models\CatalogFilterPromotion` and `App\Services\Seo\IndexationPolicy` (`catalogIndexable()` — the zero/one/many-filter decision; `activeSignatures()` cached via `Cache::rememberForever` and forgettable via `forgetCache()`, the same pattern `T-6B01`'s template cache established). `MetadataResolver` gained `resolveCatalog()` — a title/description derivation for the catalog's own virtual page (never an entity `MetadataResolver`'s other methods can resolve against), reflecting whichever of a selected object type and territory the visitor's filters name. `App\Livewire\Public\CatalogSearch::render()` now resolves and passes real metadata via `layoutData()`, and gained `activeIndexationFilters()` (one entry per active filter dimension, feeding `IndexationPolicy`). New `App\Http\Controllers\Public\RobotsController` and its `GET /robots.txt` route (registered outside the `{lang}` group — robots.txt is language-agnostic), reading a new `seo.robots_extra` portal setting (`SettingsRegistry`) appended after the fixed `Disallow: /admin` / `Disallow: /cabinet` lines.
- **Evidence:** New `tests/Feature/Public/IndexationPolicyTest.php` (7 cases, listed above). `tests/Feature/Public/PublicCatalogTest.php` (5 cases) and `tests/Feature/Public/CatalogQueryServiceTest.php` (8 cases) re-verified unaffected by the `CatalogSearch::render()` change. `PublicPerformanceBudgetTest` re-verified green with the catalog page's own cache-miss count at 26/30 — the new `IndexationPolicy` query plus one settings read, both cached indefinitely after the first call, same as `T-6B01`'s own resolution. Full non-slow suite: 691 passed, 0 failed, 3 skipped (up from 684 — 7 new, 0 regressed). Full architecture suite (8/8, `ContainmentTest` clean), `composer unused` (0 unused), and `composer audit` (clean) re-verified.

### [T-6B03] Structured data and breadcrumbs, gated on module state

- **Spec:** l1-seo.md §3.4, §5.4
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest tests/Feature/Public/StructuredDataTest.php` — 7 cases: an accommodation object emits a `LodgingBusiness` entity, a dining object a `FoodEstablishment`, an attraction object a `Place`; an object page never emits `makesOffer` availability while the booking module is inactive for that object, and does emit it once the module is active; a territory page emits a `Place` entity plus an `ItemList` of its contained objects; a page below home carries a `BreadcrumbList`, the home page carries none. Full non-slow suite: 698 passed (up from 691), 0 failed, 3 skipped. `composer analyse`, `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass. Object page cache-miss query count: 67 (within the 70 regression guard `T-6A01`'s own perf test already carries for this page, +1 from the new booking-module check).
- **Handoff:** `T-6B04` (sitemaps) and `T-6T01` (the indexation invariant) do not consume this task's own output directly, but both render pages this task now decorates — a future structured-data regression would surface as a sitemap or invariant test failure only indirectly, not something either task's own suite currently asserts on. `T-6B05`'s administration screen is the first to give `object_types.structured_data_kind` a dedicated view beyond the plain `Select` this task adds to the existing type form.
- **Notes:** The accommodation/dining/attraction split this task's structured data needs cannot be derived from anything the catalog/profile system already exposes (`has_rooms`, `has_availability_status`) — those two booleans distinguish "has room categories" from "has a simple vacancy toggle", not "is this a place to sleep, eat, or visit". Branching on the object type's own `key` string was the obvious shortcut and the one explicitly rejected: `ObjectProfilePresenter`'s own docblock states outright that adding a type must never require a code change, and `l1-object-catalog.md` independently names "a separate spec per object type" as the anti-pattern this project's whole type-registry design exists to avoid. The real fix is a new declared capability, `object_types.structured_data_kind` (`lodging`/`food`/`place`, admin-editable, defaulting to `place` — Schema.org's own most generic type), the same shape `has_rooms` already is, seeded correctly for all eight launch types and backfilled for any pre-existing row. A second, real correctness gap found only by tracing the object page's own caching: `ObjectProfilePresenter::present()` calls `loadMissing('objectType.translations')` itself, but only actually *runs* on a cache miss (`Cache::remember`'s closure) — a naive implementation reading `$object->objectType` after the cache lookup would read an unloaded relation on every cache hit, silently defaulting every object to generic `Place` markup on the far more common request path. Fixed by loading `objectType` explicitly in the controller itself, ahead of and independent of the cache lookup. A background security review (not this task's own test suite) caught a real XSS vector after the initial implementation: the JSON-LD `<script>` blocks were encoded with `JSON_UNESCAPED_SLASHES`, so any field value containing the literal text `</script>` — an object name, a promotion summary, both owner- or admin-authored, lower-trust than portal-authored copy — would close the tag early and inject arbitrary markup into the page. Fixed by switching to the `JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT` flag set, which every JSON-LD consumer (search engines, structured-data validators) accepts identically to the unescaped form.
- **Changes:** New migration `add_structured_data_kind_to_object_types_table` (string column, default `place`, backfills the eight seeded launch types by key). `ObjectTypeSeeder` and the admin `ObjectTypeForm` (a new required `Select`) both declare it going forward. New `App\Services\Seo\StructuredDataBuilder` — `forObject()` (branches on `structured_data_kind`, never on `key`; withholds `makesOffer` unless the booking module is active for that object), `forTerritory()` (`Place` + `ItemList`, built from the same catalog blocks the page already fetches — no new query), `forArticleLike()` (shared by article and news, an identical shape per spec), `forPromotion()`. `components/layouts/public.blade.php` gained a `:structuredData` prop and now auto-derives a `BreadcrumbList` from the `:breadcrumbs` prop every page already passes, so no controller builds that block itself. Five public controllers (`ObjectPageController`, `TerritoryPageController`, `BlogController`, `NewsController`, `PromotionController`) now resolve and pass their own entity's structured data; `ObjectPageController` additionally resolves `ModuleResolver::isEnabled('booking', …)` for the gate.
- **Evidence:** New `tests/Feature/Public/StructuredDataTest.php` (7 cases, listed above). `PublicPerformanceBudgetTest` re-verified: territory page unchanged at 30/30 (the `ItemList` build reuses already-fetched objects), object page at 67/70 (a genuine +1 for the new module check, well inside the regression guard `T-6A01`'s own task already documented as pre-existing, unmet-30 debt). Full non-slow suite: 698 passed, 0 failed, 3 skipped (up from 691 — 7 new, 0 regressed) — re-verified clean on a second, uncontended run after an initial run collided with a concurrently-still-running background suite (`STATE.md`'s own documented deadlock/dropped-table failure mode, not a real regression; the specific failing tests passed cleanly once nothing else was touching the test database). Full architecture suite (8/8, `ContainmentTest` clean), `composer unused` (0 unused), and `composer audit` (clean) re-verified.

### [T-6B04] Sitemap index — paginated per-language artefacts, regenerated by job

- **Spec:** l1-seo.md §5.5, implementation note 2
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest tests/Feature/Public/SitemapGenerationTest.php` — 5 cases: the index references one child per active language and populated entity type; a child sitemap paginates into a second file once its own URL count passes the configured per-file limit, each page carrying exactly the URLs it should; a territory explicitly marked not indexable never appears in any generated child; the index and a child both serve correctly through their static-read routes; both routes 404 before the generation job has ever run, rather than computing on demand. Full non-slow suite: 703 passed (up from 698), 0 failed, 3 skipped. `composer analyse`, `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass.
- **Handoff:** `T-6T01` (the indexation invariant) is the natural place to later assert this task's own promise holistically — "nothing non-public is ever indexable" extends naturally to "nothing non-public is ever in a sitemap" — but this task's own suite already covers the specific `seo_indexable`-false case per entity type it touches. `T-6B05`'s administration screen is the first place an SEO specialist can trigger an out-of-band regeneration or inspect the last run's own timestamp; today that only happens on the hourly schedule.
- **Notes:** Installing `spatie/laravel-sitemap` first required resolving a pre-existing `composer.lock`/`composer.json` mismatch (`astrotomic/laravel-translatable` and `mockery/mockery` constraints had been raised in `composer.json` without a corresponding `composer update`) — exactly the failure mode already recorded in `STATE.md`'s own Blocking Constraints; fixed with a targeted `composer update <package> --with-all-dependencies` on just those two packages before installing the new one. The per-file URL limit (sitemaps.org's own ceiling is 50,000 URLs or 50MB, whichever comes first) is a config value (`config/sitemap.php`), not a hardcoded constant — proving the pagination logic actually chunks correctly would otherwise require seeding tens of thousands of rows in a fast test; overriding the config to a small number in the test itself proves the same logic with five. `public/storage` — the symlink `Storage::disk('public')`'s URLs normally resolve through — does not exist in this container (a pre-existing, unrelated environment gap; nothing in this task depends on it, since every sitemap URL, index and child alike, is served through an explicit route reading the disk directly, never through the symlink or a raw `Storage::url()` call). Deliberately excludes the typed-catalog page (`{settlement}/{type}`) from every sitemap: it is a territory-and-type composite, not a persisted entity with a stable identity to enumerate, and stays reachable through the territory sitemap entry and the portal's own internal links regardless. "Regenerated by job" is built as an hourly schedule, not an event hook on every publish/unpublish/edit across five separate content-lifecycle services (object, territory, article, news, promotion) — the specification's own wording ("triggered by content change") more literally implies the latter, but wiring five separate services to dispatch this job would be real, spread-out coupling for an artefact that tolerates a bounded staleness window far better than it tolerates that coupling; recorded here as a deliberate scope narrowing, not an oversight.
- **Changes:** New `config/sitemap.php` (`urls_per_file`, `disk`). New `App\Services\Seo\SitemapBuilder` — one query-builder method per entity type (territory, object, news, article, promotion), each mirroring that entity's own publication rule (status, moderation, date window) plus the shared `seo_indexable` check, built as plain query-builder conditions rather than hydrated Eloquent models since a single generation run can touch every indexable row in the portal. New `App\Jobs\GenerateSitemapsJob` (`Schedule::job(...)->hourly()` in `routes/console.php`, alongside the project's other scheduled sweeps) and `App\Http\Controllers\Public\SitemapController` (`index()`/`child()`, both a plain disk read, 404 if the job has not run yet). Two new routes, `GET /sitemap.xml` and `GET /sitemaps/{locale}/{filename}`, registered outside the `{lang}` group — `ResolvePublicLocale`/`ResolveRedirect` have nothing to resolve for a machine-readable XML file. `RobotsController` (`T-6B02`) now appends a `Sitemap:` directive pointing at the index.
- **Evidence:** New `tests/Feature/Public/SitemapGenerationTest.php` (5 cases, listed above). Full non-slow suite: 703 passed, 0 failed, 3 skipped (up from 698 — 5 new, 0 regressed). Full architecture suite (8/8, `ContainmentTest` clean), `composer unused` (`spatie/laravel-sitemap` correctly detected as used, 0 unused overall), and `composer audit` (clean) re-verified.

### [T-6B05] SEO administration screen and health warnings

- **Spec:** l1-seo.md §2, §5.3
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest --filter=SeoAdministrationTest` — 7 cases: each of the six warning types (missing title, missing description, over-length title, excluded from indexing, duplicate canonical address, missing translation) fires against a deliberately broken object-type fixture, and the dashboard is reachable for a user holding `seo.view` and refused (403) for one without it. Full non-slow suite: 714 passed, 0 failed, 3 skipped. `composer analyse` (PHPStan level 8), `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass.
- **Handoff:** None within this phase — Track B (metadata, indexation, structured data, sitemaps, and now their own administration) is now fully complete infrastructure. `T-6T01` (indexation invariant) and `T-6T03` (redirect permanence) exercise this track's own promises directly rather than building new ones.
- **Notes:** Of the surface `l1-seo.md` §5.3 names, most already had a home: per-entity title/description/canonical/robots/Open Graph on each entity's own form (`T-6B01`), the sitemap route (`T-6B04`), redirects (`T-6A02`) — extended here only with the `settings.*` grant `seo_specialist` needed to reach `RedirectResource`, since that resource's permission prefix predates this task. Two pieces had no home anywhere in the codebase and needed one: the `seo_metadata_templates` table (`T-6B01`'s own middle ladder rung) had a schema and a reader but no writer — administrator-editable only by direct database access until this task's `SeoMetadataTemplateResource` gave it one; the catalog filter allowlist (`T-6B02`'s `catalog_filter_promotions`) was in the same state, closed by `CatalogFilterPromotionResource`. "Custom error pages" (`[TZ]` §13, §126, named explicitly and twice in the spec) had no infrastructure at all — no table, no admin surface, and only one real error view in the whole codebase (`resources/views/errors/404.blade.php`, static translation-key copy). Scoped deliberately to that one page rather than inventing 403/500/503 templates nothing in the product yet renders: new `ErrorPage`/`ErrorPageTranslation` models (translatable, per-language title/body), an `ErrorPageResolver` consulted by the 404 view ahead of its static fallback, and an `ErrorPageResource` to edit it. Two real bugs surfaced only by running the actual suite, not by review. First: PHP 8.3's `#[\Override]` attribute is invalid on a Filament page's `afterCreate()`/`afterSave()` — these are not real inherited methods, `BasePage::callHook()` discovers them by name via `method_exists()` alone, so the attribute claims an override that does not exist and fatals the whole request. Caught by `migrate:fresh --seed` failing outright before any test even ran; fixed by dropping `#[Override]` from every such hook across the four pages that had it (the genuine method overrides — `mutateFormDataBeforeFill`, `handleRecordCreation`, `handleRecordUpdate` — correctly keep it). Second, a recurrence of a documented pattern: making `ErrorPage` implement `Translatable` swept it into `TranslatableEntityRegistry`'s reflection-based discovery immediately, and `TranslationCompletenessReport` failed querying a `needs_review` column `error_page_translations` did not have — the exact `SQLSTATE[42703]` the `room_translations` migration's own docblock already predicted recurring for the next translatable model that skipped the convention. Fixed the same way: `needs_review`/`published_at` added directly in the table's own creation migration rather than a follow-up patch, since the table did not exist in any shared environment yet.
- **Changes:** `PermissionSeeder` gained `seo` as a resource, generating `seo.{view,create,edit,publish,delete,export}`; `RoleSeeder`'s pre-existing `seo_specialist` role now holds `seo.*` plus `settings.view`/`settings.edit` (for `RedirectResource` and the object-type registry's own SEO fields). New Filament `seo` navigation group, positioned between geography and governance. New `App\Models\SeoMetadataTemplate` (gained a `language()` relation and its first admin UI: `SeoMetadataTemplateResource`, cache-invalidating both the pre- and post-edit `(entity_type, locale)` key on save) and `App\Models\CatalogFilterPromotion` (gained `CatalogFilterPromotionResource`, invalidating `IndexationPolicy`'s cache on write). New `App\Models\ErrorPage`/`ErrorPageTranslation` (translatable per-language title/body), `App\Services\Seo\ErrorPageResolver` (cached lookup, including a cached-miss shape so an uncustomized status code is not re-queried every request), and `ErrorPageResource`; `resources/views/errors/404.blade.php` now consults the resolver before its static fallback. New `App\Services\Seo\SeoHealthReport` — the six checks, each a plain query-builder scan across every SEO-bearing translation table (territory, object type, object, promotion, news item, article), reusing the existing `TranslationCompletenessReport` for the missing-translation count rather than duplicating it. New `App\Filament\Admin\Pages\SeoHealthDashboard` (`seo.view`-gated), rendering the six checks as summary counts linking to their own detail table. `PanelAuthorizationMatrixTest` updated (three new axis-less resources, its own reflection-driven set and probe-row fixtures) per that test's own stated purpose — a new global resource is not allowed to silently go uncovered.
- **Evidence:** New `tests/Feature/Admin/SeoAdministrationTest.php` (7 cases, listed above) and `tests/Feature/Public/ErrorPageOverrideTest.php` (4 cases: override wins over the static fallback, a language the override does not cover still falls back, an edit's cache invalidation takes effect on the next request, and an uncustomized status code's absence is itself cached). `tests/Feature/Admin/PanelAuthorizationMatrixTest.php` re-verified with the three new resources added to its own reflection-driven expected set (5/5, 87 assertions). Full non-slow suite: 714 passed, 0 failed, 3 skipped (up from 703 — 11 new, 0 regressed). Full architecture suite (8/8, `ContainmentTest` clean), `composer unused` (0 unused), and `composer audit` (clean) re-verified. `php artisan migrate:fresh --seed` re-verified applying cleanly from empty after both bug fixes.

### [T-6C01] Derived figures across every aggregation dimension

- **Spec:** l1-analytics.md §5.3
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest --filter=PortalReportingTest` — 3 cases: one assertion per derived figure against a fixed fixture with known counts, plus a query-log assertion proving no query issued to compute any of the eight figures references `stat_events`. Full non-slow suite: 717 passed, 0 failed, 3 skipped. `composer analyse` (PHPStan level 8), `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass. `AnalyticsReport` page re-measured against `DemoVolumeSeeder` volume (52,800 objects, 6,270 territories): 18/30 queries, comfortably within budget.
- **Handoff:** `T-6C02` (traffic-source and page-popularity reporting) extends the same `AnalyticsReport` back-office screen next — no rework expected, since this task's derived figures and that task's own additions are independent sections of the same page. `T-6T01`/`T-6T02` do not consume this task's output; portal-wide reporting is a read surface with nothing downstream depending on its own figures.
- **Notes:** Three of the eight figures (most viewed objects, most popular categories, banner click-through rate) are genuinely event-derived and read `stat_dailies` through `AnalyticsReportingService::scopedQuery()` directly, so their dimensional scoping (territory, country, object type, locale, period) never drifts from the rest of the statistics screen. The other five (new owner, new object, bump, published promotion, pending moderation counts) are not event-derived at all — `[TZ]` §89/§125 groups them in the same sentence as the event-derived three, but each reads its own operational table (`users`, `objects`, `bump_events`, `promotions`, `moderation_requests`) directly, scoped by period only; extending them to the full dimensional filter set would mean joins several of those tables do not carry columns for, which the specification does not ask for explicitly. Pending moderation is a deliberate exception to "period-scoped": a moderation queue's depth is a live gauge answering "what needs attention now", not a historical count, matching the exact reading `DashboardMetrics::operational()` already gives the same figure elsewhere in this codebase — period-filtering it would have quietly changed its meaning. A real regression surfaced only by running the full suite, not by this task's own new test: `newOwnerCount()`'s first draft used Spatie's `User::role('object_owner')` scope, which resolves the named role eagerly and throws `RoleDoesNotExist` when nothing has seeded it — `AnalyticsReportingTest`'s own pre-existing permission-gate test does not seed that role, so loading `AnalyticsReport` there 500'd. Fixed by replacing the scope with a plain `whereHas('roles', fn ($q) => $q->where('name', 'object_owner'))`, which degrades to zero instead of throwing — the more defensive choice for a report figure that must never fail a page render over an unrelated fixture's own omission.
- **Changes:** New `App\Services\Analytics\PortalReportingService` (`derivedFigures()` plus one method per figure), consuming `AnalyticsReportingService::scopedQuery()` for the three event-derived figures rather than duplicating its filter logic. `AnalyticsReport` (Filament Admin page) gained `derivedFigures()`, rendered as six summary cards plus two ranked lists (most viewed objects, most popular categories) beneath the existing per-kind summary grid, reusing the same filter state the page already collects.
- **Evidence:** New `tests/Feature/Admin/PortalReportingTest.php` (3 cases, listed above). `tests/Feature/AnalyticsReportingTest.php` re-verified passing after the `RoleDoesNotExist` fix (7/7). `tests/Feature/Admin/ContentAndCommercePanelBudgetTest.php` (`slow` group) re-run at seeded volume: `AnalyticsReport` at 18/30 queries — no N+1 introduced by the two new name-lookup queries (`object_translations`/`object_type_translations`, batched per report render, not per row). Full non-slow suite: 717 passed, 0 failed, 3 skipped (up from 714 — 3 new, 0 regressed). Full architecture suite (8/8, `ContainmentTest` clean), `composer unused` (0 unused), and `composer audit` (clean) re-verified.

### [T-6C02] Traffic-source and page-popularity reporting in the back office

- **Spec:** l1-analytics.md §5.6
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest --filter=TrafficSourceReportTest` — 4 cases: all six channels aggregate correctly, zero-filled where nothing was recorded; the internal channel is reported distinctly from referral; the period filter excludes events outside the window; the result is always a fixed six-key channel count — grouped at the SQL level, never a per-visitor row, and no export action in the codebase is bound to raw `StatEvent` rows. Full non-slow suite: 721 passed, 0 failed, 3 skipped. `composer analyse` (PHPStan level 8), `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass. `AnalyticsReport` re-measured against `DemoVolumeSeeder` volume: still 18/30 queries.
- **Handoff:** Track C is now fully complete — both `T-6C01` and `T-6C02` land on the same `AnalyticsReport` back-office page, and nothing in Track D or Track T consumes either task's output. Page popularity itself needed no new work here: `T-6C01`'s own "most viewed objects" section already satisfies `[TZ]` §23's page-popularity requirement on this exact screen; this task's own scope narrowed to the traffic-source half only.
- **Notes:** The one design decision this task turned on was already made and documented by an earlier task, not invented here: `ObjectStatisticsService` (the owner cabinet's own traffic-source breakdown, built before this phase) already established that `stat_dailies`' rollup grain deliberately excludes the source columns — "a fixed rollup grain is a documented invariant of the two-tier analytics model" per that service's own docblock. Extending the aggregate table to carry `source_channel` would have reversed that decision without a documented reason to, and would still leave `source_domain`/`source_campaign` off the aggregate regardless (raw-tier-only by the same reasoning). The new `TrafficSourceReportingService` therefore mirrors `ObjectStatisticsService::trafficSources()`'s own choice exactly: reads raw `stat_events`, one deliberate, narrow exception to this phase's otherwise-universal "aggregate tier only" rule, always pre-grouped by channel so the coarse shape `TrafficSourceRecorder` itself enforces (never a full referrer URL, never a per-visitor identity) survives all the way to the report. Six channels are always returned, zero-filled, matching the same always-every-key shape `AnalyticsReportingService::summary()` already established for event kinds — a channel that recorded nothing in the filtered window is a fact worth showing, not an absent key.
- **Changes:** New `App\Services\Analytics\TrafficSourceReportingService` (`byChannel()` — one query-builder scan over `stat_events`, grouped by `source_channel`, period/territory/country/locale filterable). `AnalyticsReport` gained `trafficSourceBreakdown()`, rendered as a third card alongside the existing most-viewed-objects and most-popular-categories lists (grid widened to three columns), reusing the same filter state the rest of the page already collects.
- **Evidence:** New `tests/Feature/Admin/TrafficSourceReportTest.php` (4 cases, listed above). `tests/Feature/AnalyticsReportingTest.php` and `tests/Feature/Admin/PortalReportingTest.php` re-verified passing (10/10, no regression from the additional page query). `tests/Feature/Admin/ContentAndCommercePanelBudgetTest.php` (`slow` group) re-run at seeded volume: `AnalyticsReport` unchanged at 18/30 queries. Full non-slow suite: 721 passed, 0 failed, 3 skipped (up from 717 — 4 new, 0 regressed). Full architecture suite (8/8, `ContainmentTest` clean), `composer unused` (0 unused), and `composer audit` (clean) re-verified.

### [T-6D01] API module gate, versioned routing, and the disabled-capability 404

- **Spec:** l1-public-api.md §3.1, §3.3, §5.3
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest --filter=ApiModuleGateTest` — 4 cases: an existing endpoint (`/api/v1/status`) 404s while the module is disabled; a never-registered path 404s identically, same status code, so the two are indistinguishable; the existing endpoint serves normally once an administrator enables the module at portal scope; the never-registered path still 404s regardless. Full non-slow suite: 725 passed, 0 failed, 3 skipped. `composer analyse` (PHPStan level 8), `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass.
- **Handoff:** `T-6D02` (token model) builds the auth layer this task's own gate sits in front of — every future business endpoint in `routes/api_v1.php` gets `module:api`, then token validation, in that order, matching §5.3's own request-flow diagram. `T-6D03` (read endpoints) and `T-6D04` (rate limiting, documentation) both extend this same route file and registration; neither needs to touch the gate itself.
- **Notes:** The gate needed no new mechanism at all — `App\Http\Middleware\EnsureModuleEnabled` and its `module` alias already existed (built for a different gated route, `object`-scoped bookings), already 404s on a disabled module, and was already registered in `bootstrap/app.php`. Applying `module:api` to the whole `routes/api_v1.php` group was the entire gate; the "indistinguishable from unregistered" half of the requirement falls out of ordinary Laravel routing for free — a path with no matching route pattern 404s before any middleware runs, and a path that matches but sits behind a disabled module 404s from the middleware, so both arrive at the same status code without any code coordinating them to agree. The one genuine design call was what "an existing endpoint" should be, since `T-6D02` (tokens) and `T-6D03` (read endpoints) have not landed yet: a full business endpoint from the spec's own launch list would need auth infrastructure this task does not build, so it uses a permanent, deliberately auth-free `GET /api/v1/status` liveness/version check instead — the kind of endpoint most versioned public APIs carry outside their own business-token gate, not a throwaway stub the next task deletes. `bootstrap/app.php`'s `withRouting()` had no `api:` parameter at all before this task; registering it also activates the framework's existing `shouldRenderJsonWhen($request->is('api/*'))` exception rendering that was already configured but had nothing to apply to.
- **Changes:** `bootstrap/app.php` gained `api: routes/api_v1.php` and `apiPrefix: 'api/v1'` in `withRouting()`. New `routes/api_v1.php` (the whole group behind `module:api`) and `App\Http\Controllers\Api\V1\StatusController` (`GET /status`, no auth, `{"status":"ok","version":"v1"}`). `ModuleSeeder` gained the `api` module — `default_state: disabled`, `scopable_levels: ['portal']` only, no dependencies. `tests/Feature/RegistrySeedersTest.php` updated for the fifth seeded module and its disabled default state, per that test's own exact-count assertions.
- **Evidence:** New `tests/Feature/Api/ApiModuleGateTest.php` (4 cases, listed above). `tests/Feature/RegistrySeedersTest.php` re-verified passing with the updated module count and state assertions (4/4). `tests/Feature/Admin/ModuleAdministrationTest.php` re-verified unaffected — it seeds its own module fixture directly rather than reading the real registry. Full non-slow suite: 725 passed, 0 failed, 3 skipped (up from 721 — 4 new, 0 regressed). Full architecture suite (8/8, `ContainmentTest` clean), `composer unused` (0 unused), and `composer audit` (clean) re-verified. `php artisan migrate:fresh --seed` re-verified applying cleanly from empty.

### [T-6D02] API client and token model — issuance, scoping, revocation, journalling

- **Spec:** l1-public-api.md §5.2, §5.5, §6
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest --filter=ApiTokenLifecycleTest` — 4 cases: only the token hash is ever stored, never the clear value; a revoked token is rejected on its very next request; issuance, revocation, and every scope change appear in the action journal; the country/category scope resolves into the same `ScopeConstraint` shape an administrator grant does. Full non-slow suite: 729 passed, 0 failed, 3 skipped. `composer analyse` (PHPStan level 8), `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass.
- **Handoff:** `T-6D03` (read endpoints) consumes `ApiTokenService::scopeConstraint()` to narrow `CatalogQueryService` results the same way a scoped administrator's list query is narrowed, and gates each endpoint's own resource ability via Sanctum's native `tokenCan()` against the `App\Support\Api\ApiResource` values this task defined. `T-6D04` (rate limiting) reads the `rate_limit_per_minute` column this task's token model already carries; no further schema change expected.
- **Notes:** Two real bugs surfaced only once a Sanctum-guarded route actually existed alongside the module gate, not from this task's own new test: (1) `EnsureModuleEnabled` called `$request->user()?->getAuthIdentifier()` unconditionally to build owner-scope context, which fatals when the resolved actor is an `ApiClient` rather than the portal's own `User` — fixed with a type guard, since an API client's own ID has nothing to do with `objects.owner_id`. (2) Laravel's default middleware priority runs `auth:sanctum` ahead of any custom alias not itself in that priority list, which put token authentication before the module gate — the reverse of the specification's own request-flow order, where a disabled module must be inert before any bearer token is inspected. Fixed via `prependToPriorityList()` in `bootstrap/app.php` rather than reordering the route's own middleware array, since route-level order alone cannot override framework priority sorting. The country/category scope storage deliberately mirrors `role_scopes`' shape (`scope_kind` + a soft-referenced `scope_reference_id`) but omits both its `territory` axis and its `none` sentinel row — a token has no territory-subtree axis per the specification's own token-model diagram, and "unrestricted" is simply the absence of any row for an axis rather than a row that says so. The `resources` axis reuses Sanctum's own `abilities` column instead of a third scope kind, so "which endpoints" gates through Sanctum's native `tokenCan()` rather than a bespoke check.
- **Changes:** New `App\Models\{ApiClient,ApiToken,ApiTokenScope}` (`ApiToken` extends `Laravel\Sanctum\PersonalAccessToken`, registered via `Sanctum::usePersonalAccessTokenModel()`, with `findToken()` overridden to reject a revoked token). New `App\Support\Api\ApiResource` enum (the nine launch-surface resource families). New `App\Services\Api\ApiTokenService` (`issue`/`revoke`/`changeScope`/`scopeConstraint`, journalling the first three). New `App\Filament\Admin\Resources\ApiClients\ApiClientResource` with a `TokensRelationManager` (issue, edit scope, revoke — the plain-text token surfaces once via a persistent notification, never stored). New `App\Http\Controllers\Api\V1\TokenController` (`GET /api/v1/token`, `auth:sanctum`, self-introspection). Two migrations: `revoked_at`/`revoked_by`/`rate_limit_per_minute` on `personal_access_tokens`, and the new `api_token_scopes` table. `config/auth.php` gained the `sanctum` guard (never registered before this task). `PermissionSeeder` gained the `api` resource verb set. `bootstrap/app.php` gained the module-gate-before-auth middleware priority fix.
- **Evidence:** New `tests/Feature/Api/ApiTokenLifecycleTest.php` (4 cases, listed above). `tests/Feature/Api/ApiModuleGateTest.php`, `tests/Feature/EnsureModuleEnabledTest.php`, and `tests/Feature/ModuleInertnessTest.php` re-verified passing after the middleware priority and type-guard fixes (9/9 combined). `tests/Feature/Admin/PanelAuthorizationMatrixTest.php` updated with the new resource's probe-row fixture and added to the axis-less-registry list (5/5). Full non-slow suite: 729 passed, 0 failed, 3 skipped (up from 725 — 4 new, 0 regressed). Full architecture suite (8/8, `ContainmentTest` clean), `composer unused` (0 unused, the `laravel/sanctum` exclusion retired now that the package is genuinely used), and `composer audit` (clean) re-verified. `php artisan migrate:fresh --seed` re-verified applying cleanly from empty.

### [T-6D03] Read endpoints as a layer over the existing catalog retrieval contract

- **Spec:** l1-public-api.md §5.1, §5.3, §6
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest --filter=ApiReadContractTest` — 6 cases: no token rejected; a token whose scope lacks the resource ability rejected; the objects collection matches `CatalogQueryService`'s own tier-first order exactly; a pending, rejected, archived, or hidden object is absent from the collection and 404s at the detail endpoint; a country-scoped token is narrowed to its own country on both the collection and `/countries`; news/promotions/articles list published-only. `composer analyse` (PHPStan level 8), `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass. Full non-slow suite verified once at 744/0/3 after this task and the other three landed together in the same session — see `T-6D04`'s own entry for that run.
- **Handoff:** `T-6D04` (rate limiting, consumption, documentation) wraps every endpoint this task registered. `T-6T02` (API parity invariant) consumes the exact same `CatalogQueryService::search()` call this task's `ObjectController::index()` makes, so its own parity assertion is comparing two callers of one implementation, not two implementations.
- **Notes:** `CatalogQueryService::search()` gained an optional second parameter, `?ScopeConstraint $constraint`, rather than adding scope fields to `CatalogSearchCriteria` — the criteria DTO is the surface's own display filters, and mixing an authorization concern into it would blur what every existing caller (home, territory pages, catalog search) hands it. Every public page still calls `search()` with `$constraint` left `null`; only the API's own controller resolves and passes one. `App\Services\Authorization\ResourceQueryScoper` gained a second public method, `applyConstraint()`, extracted from `narrow()`'s own body — `narrow()` now derives a `ScopeConstraint` from a `User`'s role grants and delegates to it, while the API resolves its `ScopeConstraint` from a token instead; both share the identical narrowing logic, so "what a `ScopeConstraint` means against a query" cannot drift between an administrator's grant and a token's scope. Object collection and detail responses are shaped by the *same* `ObjectCardPresenter`/`ObjectProfilePresenter` the public catalog and object pages already use, reused directly rather than re-derived — the Blade-facing URLs those presenters build (`route()` calls, `PublicUrlGenerator`) resolve correctly for an API response too, since `App\Http\Middleware\ResolveApiLocale` (new) sets the request's locale from an optional `?lang=` query parameter, falling back to the portal's primary language, before any controller runs. News/promotion country-scope narrowing reads through a new `applyCountryScopeViaTerritory()` helper rather than `ResourceQueryScoper::applyConstraint()`, since neither model carries its own `country_id` column — only a `territory_id` to join through; portal-wide news (a null `territory_id`) always passes regardless of a token's country scope, since it belongs to no single country a scope could exclude it from. Articles are visibility-gated (`Article::scopePublished()`) but deliberately not scope-narrowed by country or category — an article's own `territories()` is a many-to-many association with no single owning country, and it carries no object-type-equivalent axis at all.
- **Changes:** New `App\Http\Controllers\Api\V1\{Country,Territory,ObjectType,Amenity,Object,News,Promotion,Article}Controller` (13 routes total across the launch surface). New `App\Http\Resources\Api\V1\*` (10 `JsonResource` classes — the first use of Laravel's resource layer anywhere in this codebase). New `App\Http\Middleware\ResolveApiLocale` (aliased `api.locale`). New `App\Http\Controllers\Api\V1\Concerns\ResolvesTokenScope` trait. `CatalogQueryService::search()`/`buildQuery()`/`cacheKey()` gained the optional `ScopeConstraint` parameter; `CatalogSearchCriteria` gained `availabilityStatus` and (unused directly, but threaded for completeness) no other new fields. `ResourceQueryScoper::applyConstraint()` extracted from `narrow()`. `bootstrap/app.php` aliased Sanctum's `CheckAbilities` middleware as `abilities` (Sanctum ships the class but no route alias for it) and registered `api.locale`.
- **Evidence:** New `tests/Feature/Api/ApiReadContractTest.php` (6 cases, listed above). Full public-site suite (`tests/Feature/Public/**`, 134 tests) and `CatalogQueryServiceTest` (9 tests) re-verified passing after the `CatalogQueryService`/`ResourceQueryScoper` changes, including the query-count performance-budget test (catalog/territory/object query counts unchanged). `PanelAuthorizationMatrixTest` and `ScopeAuthorizerTest` re-verified passing after the `ResourceQueryScoper` refactor. `php artisan migrate:fresh --seed` re-verified applying cleanly from empty (no schema change this task). Full non-slow suite verified once, together with `T-6D04`/`T-6T01`/`T-6T03`, at 744 passed, 0 failed, 3 skipped (up from 729 — see `T-6D04`'s own entry).

### [T-6D04] Rate limiting, consumption measurement, and generated documentation

- **Spec:** l1-public-api.md §3.3, §5.4
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest --filter=ApiRateLimitTest` — 4 cases: a token is served up to its configured per-minute limit, then 429s carrying a `Retry-After` header; a query-log assertion proves zero queries against `objects` run on the request the limiter blocks; a token with no override falls back to the portal-wide `api.default_rate_limit_per_minute` setting; the generated document at `GET /api/v1/docs` lists every one of the 14 registered `api.v1.*` routes, each with its resource ability, its token requirement, and its exact validation parameters. Full non-slow suite: 744 passed, 0 failed, 3 skipped. `composer analyse` (PHPStan level 8), `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass.
- **Handoff:** Track D is now fully complete. `T-6T02` (API parity invariant) is the one remaining task in Phase 6 with a dependency on this track — it needs `T-6D03`'s read endpoints, not anything from this task. `T-6T01`/`T-6T03` (Track T validation) needed nothing from Track D at all and were completed independently in this same session.
- **Notes:** No API-documentation package exists in this project's dependency set, and neither `l1-public-api.md` nor `l2-tech-stack.md`'s own package table names one — installing an unlisted third-party generator for a requirement the specification states only in prose ("generated from the same schema definitions that validate requests") would be an unauthorized dependency addition for a need the route table and existing validation already satisfy. Built a minimal, hand-rolled `ApiDocumentationGenerator` instead: every controller that validates query parameters now implements a new `DocumentsQueryParameters` interface (`queryParameterRules(): array`), and its `index()` action calls `$request->validate(self::queryParameterRules())` — the identical array both the validator and the generator read, so the two cannot drift by construction, which is the literal requirement rather than an approximation of it. Consumption measurement required extending the six-kind analytics event model to a seventh, `StatEventKind::ApiRequest`, plus a new nullable `endpoint` column on both `stat_events` and `stat_dailies` (no existing column was semantically appropriate — `source_domain`/`source_campaign` are referrer-tracking columns, not a route-name field, and repurposing one would have been the "borrow an unrelated column" anti-pattern this project's own schema conventions avoid). Widening both tables' `kind` CHECK constraints required a real migration (`stat_events` stays partitioned, so the constraint is altered at the parent table and Postgres propagates it to every partition automatically) and `AnalyticsRollupJob`'s `selectRaw`/`groupBy`/insert and `stat_dailies_unique_rollup`'s own column list needed the new `endpoint` dimension added explicitly — neither aggregates generically. `EventCaptureService::capture()` was extended to skip its own visitor-window dedup token entirely for `ApiRequest` events: the existing dedup logic exists to collapse an accidental double-render of the same page within a fifteen-minute window, and applying it to API consumption would have silently undercounted a token making several genuine requests in that same window — the ordinary case this kind exists to measure, not a duplicate of itself. `RecordApiConsumption` middleware is placed to *wrap* the resource-ability gate rather than precede it: `$next()` throws before returning when the module gate, token auth, rate limit, or ability check refuses a request, which skips the capture call entirely — only a request that actually cleared every prior gate and received a real response is consumption an endpoint or token actually used, matching the specification's own request-flow diagram, where "record consumption" is the final step after the response is emitted.
- **Changes:** New named rate limiter `RateLimiter::for('api-token', ...)` in `AppServiceProvider::boot()`, keyed by token id, reading `ApiToken::rate_limit_per_minute` (set at T-6D02) or falling back to the new `api.default_rate_limit_per_minute` setting. New `App\Http\Middleware\RecordApiConsumption` (aliased `record.consumption`). New `App\Services\Api\ApiDocumentationGenerator` and `App\Http\Controllers\Api\V1\DocumentationController` (`GET /api/v1/docs`, auth-free like `/status`). New `App\Http\Controllers\Api\V1\Concerns\DocumentsQueryParameters` interface, implemented by every controller that validates query parameters. Migration `2026_08_17_090000_add_api_request_kind_and_endpoint_to_analytics_tables.php` — widens both `kind` CHECK constraints, adds `endpoint` to `stat_events`/`stat_dailies`, widens `stat_dailies_unique_rollup`. `StatEventKind::ApiRequest` case added. `CaptureStatEventJob`, `EventCaptureService::capture()`, and `AnalyticsRollupJob` all extended to carry `endpoint` end to end. `SettingsRegistry` gained the `api` group with `api.default_rate_limit_per_minute` (default 60). `routes/api_v1.php` restructured: `throttle:api-token` now wraps every authenticated route (including `/token`), `record.consumption` wraps every ability-gated business endpoint, `/docs` sits alongside `/status` outside the token gate.
- **Evidence:** New `tests/Feature/Api/ApiRateLimitTest.php` (4 cases, listed above). `tests/Feature/Api/ApiReadContractTest.php` and `tests/Feature/Api/ApiTokenLifecycleTest.php` re-verified passing after the throttle/consumption middleware insertion (10/10 combined). `tests/Feature/AnalyticsRollupAndCompactionTest.php` (5/5) and `tests/Feature/EventCaptureServiceTest.php` (8/8) re-verified passing after the seventh-kind and dedup-skip changes. Full architecture suite (8/8, `ContainmentTest` clean), `composer unused` (0 unused), and `composer audit` (clean) re-verified. `php artisan migrate:fresh --seed` re-verified applying cleanly from empty, including the new analytics migration. **Full non-slow suite, run once after this task and the other three (`T-6D03`, `T-6T01`, `T-6T03`) all landed together in the same session: 744 passed, 0 failed, 3 skipped (up from 729 before any of the four — 15 new across all four tasks combined, 0 regressed).**

### [T-6T01] Indexation invariant — nothing non-public is ever indexable

- **Spec:** l1-seo.md §3, §5.2
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest --filter=IndexationInvariantTest` — 2 cases: every registered `filament.admin.*`/`filament.cabinet.*` route name and URI is disjoint from the public `public.*`/`{lang}`-prefixed route set; a pending, rejected, archived, hidden, or soft-deleted object never appears in the generated sitemap, cross-checked directly against the XML artefact rather than only against the page's own 404/noindex behaviour. `composer analyse` (PHPStan level 8), `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass. Full non-slow suite verified once at 744/0/3, together with `T-6D03`/`T-6D04`/`T-6T03` — see `T-6D04`'s own entry.
- **Handoff:** None — this is a validation task with nothing downstream depending on its own output.
- **Notes:** A real, pre-existing bug surfaced during research for this task, not from the new test itself: `RobotsController` hardcoded `Disallow: /admin`, but `config('booking.panels.admin.path')` defaults to `portal-admin` — a deliberately non-guessable path per that config file's own documented security rationale (obscurity against credential-stuffing traffic). The hardcoded line neither matched the real, reachable path nor protected it, silently defeating the obscurity layer it was meant to reinforce. Fixed by reading both panels' paths from config instead of hardcoding either. `tests/Feature/Public/IndexationPolicyTest.php`'s own existing robots.txt assertion was updated to match (it previously asserted the same wrong literal string and would have kept passing forever without ever proving the real path was disallowed). Two of the four negative cases the task's own prose names — catalog filter-count indexability and territory-level `seo_indexable` exclusion — were already covered by `IndexationPolicyTest`/`SitemapGenerationTest` from earlier tasks and were not re-tested here to avoid duplicating passing coverage; this task's own two cases close the two gaps neither of those files reached: the sitemap-vs-moderation-state cross-check, and the route-registry disjointness sweep.
- **Changes:** `App\Http\Controllers\Public\RobotsController` reads `config('booking.panels.{admin,cabinet}.path')` instead of two hardcoded strings.
- **Evidence:** New `tests/Feature/Public/IndexationInvariantTest.php` (2 cases, listed above). `tests/Feature/Public/IndexationPolicyTest.php` updated and re-verified passing (7/7) after the robots.txt fix. Full architecture suite (8/8, `ContainmentTest` clean) re-verified. `php artisan migrate:fresh --seed` re-verified applying cleanly from empty (no schema change this task).

### [T-6T03] Redirect permanence and slug stability

- **Spec:** l1-seo.md §5.5
- **Status:** Done
- **Assignment:** Agent
- **Verify:** `docker compose exec app php -d memory_limit=1G vendor/bin/pest --filter=SlugStabilityInvariantTest` — 3 cases: reparenting a genuinely mid-tree territory (three levels: grandparent → parent → child) leaves a permanent redirect for both the reparented node's own path *and* its child's compound path, which changed only as a consequence of its ancestor moving; a two-hop rename chain collapses so both the original and the intermediate address redirect straight to the final destination, never to each other; a new territory is refused a slug an active redirect still claims. `composer analyse` (PHPStan level 8), `pint --test`, and the full architecture suite (8/8, `ContainmentTest` clean) pass. Full non-slow suite verified once at 744/0/3, together with `T-6D03`/`T-6D04`/`T-6T01` — see `T-6D04`'s own entry.
- **Handoff:** None — validation task, nothing downstream.
- **Notes:** `RedirectResolutionTest` (T-6A02) already proves each mechanism works in isolation, including its own reparent case — but that case has exactly one direct child, so "every affected descendant path" was never actually exercised at real depth; a single directly-attached child cannot distinguish "this task redirects its immediate children" from "this task redirects the whole subtree, transitively." This task's own reparent case adds a genuine grandchild specifically to close that gap. No new production code was needed: `TerritoryAdministrator::reparent()`'s existing call to `recomputeSlugPaths()` already walks `descendantsAndSelf()` on the reparented node, which already includes every descendant at any depth — the mechanism was already correct, it simply had no test proving so beyond one level.
- **Changes:** None — this task is validation-only, consistent with its own framing (a durable invariant proven holistically, not a new mechanism).
- **Evidence:** New `tests/Feature/Public/SlugStabilityInvariantTest.php` (3 cases, listed above). `tests/Feature/Public/RedirectResolutionTest.php` re-verified passing unchanged (6/6). Full architecture suite (8/8, `ContainmentTest` clean) re-verified. `php artisan migrate:fresh --seed` re-verified applying cleanly from empty (no schema change this task).
